import React from 'react';
import { BaseLayout } from '../../components/BaseLayout';

type Props = {};

export const CalculateEmissions = (props: Props) => {
  return <BaseLayout>
  <h1>Calculate emissions</h1></BaseLayout>
};
